
<div class="row">
  <div class="col-md-5">
    <h1>Courses#index</h1>

    <% @courses.each do |course| %>
    <p>
      <%= link_to course.field, course_path(course) %>
    </p>
    <% end %>

  </div>

  <div class="col-md-4">
      <h2>Student registered</h2>
  </div>

</div>
